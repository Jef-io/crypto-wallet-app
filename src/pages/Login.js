import React from 'react';
// import { loginRequest } from '../utils/loginService'
// import { useHistory } from 'react-router';

export default function Login() {

//   let history = useHistory();

//   const login = async ({username, password}) => {
    
//     const identifiers = {
//         username: username,
//         password: password
//     }
//     // console.log(identifiers);

//     const loginreq = await loginRequest(identifiers);
    
//     if (!loginreq) console.log('errror')
//     history.push('/');

//   }

  return (
    <main className="Login">
        LOGIN
    </main>
  );
}